# Heavy, optional. Without these the service uses the rule-based annotator
# and reports nlp.backend == "rules" on /api/health.
torch==2.5.1
transformers==4.47.1
sentencepiece==0.2.0

# --- topic discovery (BERTopic) --------------------------------------------
# Version lock, learned the hard way: umap-learn and hdbscan call
# sklearn's check_array(force_all_finite=...), which was renamed in
# scikit-learn 1.6 and removed in 1.9. umap 0.5.12 uses the NEW name and
# hdbscan 0.8.40 the OLD one, so they cannot both work against one sklearn.
# This trio agrees:
scikit-learn==1.5.2
umap-learn==0.5.7
hdbscan==0.8.40
bertopic==0.16.4
sentence-transformers==3.3.1
