# --- core service -----------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
feedparser==6.0.11

# --- forecasting ------------------------------------------------------------
# 0.15.0 specifically: 0.14.4 fails to import against pandas 3 (a changed
# deprecate_kwarg signature).
statsmodels==0.15.0

# --- network analysis -------------------------------------------------------
# PageRank, Louvain and betweenness are computed, not authored. scipy is what
# networkx uses for the sparse-matrix PageRank solver.
networkx==3.4.2
scipy==1.14.1

# --- stores (all optional: the service falls back to in-memory) -------------
redis==5.2.1
asyncpg==0.30.0
neo4j==5.27.0
qdrant-client==1.12.2

# --- NLP (optional: rule-based annotator is used when these are absent) -----
# Install with:  pip install -r requirements-nlp.txt
